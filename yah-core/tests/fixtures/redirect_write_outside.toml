command = "echo hello > /etc/out.txt"
capabilities = ["write-outside-repo"]
notes = "Write redirect to file outside repo"

[context]
cwd = "/home/user/project"
project_root = "/home/user/project"
home = "/home/user"
