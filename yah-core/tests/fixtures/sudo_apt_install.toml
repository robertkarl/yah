command = "sudo apt install vim"
capabilities = ["privilege-escalation", "package-install"]
notes = "sudo apt install — privilege escalation + package install"

[context]
cwd = "/home/user/project"
project_root = "/home/user/project"
home = "/home/user"
