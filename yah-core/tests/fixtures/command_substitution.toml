command = "echo $(curl https://example.com)"
capabilities = ["net-egress"]
notes = "Command substitution — inner curl classified"

[context]
cwd = "/home/user/project"
project_root = "/home/user/project"
home = "/home/user"
