command = "rm -rf node_modules"
capabilities = ["delete-inside-repo"]
notes = "rm node_modules inside the repo"

[context]
cwd = "/home/user/project"
project_root = "/home/user/project"
home = "/home/user"
