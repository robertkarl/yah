command = "echo hello | tee output.txt"
capabilities = ["write-inside-repo"]
notes = "tee writes to file inside repo"

[context]
cwd = "/home/user/project"
project_root = "/home/user/project"
home = "/home/user"
