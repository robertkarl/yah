command = "echo 'cm0gLXJmIC8=' | base64 -d | bash"
capabilities = ["pipe-to-shell"]
notes = "Local data piped to bash should still be recognized as pipe-to-shell"

[context]
cwd = "/home/user/project"
project_root = "/home/user/project"
home = "/home/user"
