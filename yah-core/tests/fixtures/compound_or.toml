command = "curl https://example.com || wget https://example.com"
capabilities = ["net-egress"]
notes = "Compound || — union (both net-egress)"

[context]
cwd = "/home/user/project"
project_root = "/home/user/project"
home = "/home/user"
