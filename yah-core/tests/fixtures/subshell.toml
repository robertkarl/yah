command = "(curl https://example.com)"
capabilities = ["net-egress"]
notes = "Subshell — classify inner command"

[context]
cwd = "/home/user/project"
project_root = "/home/user/project"
home = "/home/user"
